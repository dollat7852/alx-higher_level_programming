#include "lists.h"
/**
 *
 */
listint_t *insert_node(listint_t **head, int number)
{
	listint_t *tmp, *current;
	current = head;
	listint_t new_l = malloc(sizeof(listint_t);

	if (head == NULL || new == NULL)
		return (NULL);
	while(current != NULL)
	{
		if (current.n < number)
			current.next;
		tmp= current.n;
		new_l = current.next;
		current = new;	

	}

	return current;
}
